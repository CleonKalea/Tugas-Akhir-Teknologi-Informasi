import React, { useState } from 'react';
import './History.css';

const History = ({  }) => {

  return (
    <div className="history">
      <div className="history-header">
        <span className="history-title-1">Prediction History</span>
      </div>
    </div>
  );
};

export default History;
